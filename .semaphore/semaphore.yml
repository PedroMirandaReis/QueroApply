version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Docker
    task:
      jobs:
        - name: docker build
          commands:
            - checkout
            - 'sudo echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin'
            - 'sudo docker pull "${DOCKER_USERNAME}/queroapply:latest" || true'
            - 'sudo docker build --cache-from -t "${DOCKER_USERNAME}/queroapply:latest" -f "${DOCKER_USERNAME}/queroapply:latest" .'
            - 'sudo docker push "${DOCKER_USERNAME}/queroapply:latest"'
      secrets:
        - name: dockerhub
      agent:
        machine:
          type: e1-standard-2
          os_image: ubuntu1804
