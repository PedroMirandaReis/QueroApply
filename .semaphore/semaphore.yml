version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Docker
    task:
      jobs:
        - name: docker build
          commands:
            - checkout
            - 'sudo echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin'
            - 'docker pull "${DOCKER_USERNAME}/QueroApply:latest" || true'
            - 'docker build --cache-from "${DOCKER_USERNAME}/QueroApply:latest" -t "${DOCKER_USERNAME}/QueroApply:latest" .'
            - 'docker push "${DOCKER_USERNAME}/QueroApply:latest"'
      secrets:
        - name: dockerhub
      agent:
        machine:
          type: e1-standard-2
          os_image: ubuntu1804
